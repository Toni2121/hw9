# global inside a function means that you're able to take the variable that is outside the function
# and change it.

# using global is a bad practice. instead we use return.

# we will get an error in this function because we get the x after it got printed.